import { Container, Title } from "@mantine/core";

export default function Account() {
  return (
    <Container mt="xl">
      <Title order={1} mb="lg">
        Account
      </Title>
    </Container>
  );
}
