language: node_js
node_js:
  - "lts/*"

cache:
  directories:
    - "$HOME/.npm"

script:
  - cd client
  # Install npm dependencies
  - npm ci
  # Run prettier format check
  - npm run format:check
  # Run linter
  - npm run lint
  # Run tests
  - npm test
